"use strict";
describe('Mapping', () => {
    describe('field()', () => {
        it('should set metadata on the class concerning the field', () => {
            // console.log(Reflect.getOwnMetadataKeys(resource.Field));
            //
            // let meta    = MetaData.forTarget(resource.Field).fetch('mappingInstance');
            // let something = MetaData.forTarget(resource.Field).fetchOrPut('mappingInstance', new Mapping(resource.Field));
            //
            //
            //
            // let mapping = Mapping.forEntity(resource.Field);
            //
            //
            // // @todo test
            // mapping.field('ikbeneensupergezelligedate', {type: 'sexy', echtwaar: true});
            //
            // let meta   = MetaData.forTarget(resource.Field).fetch('mapping');
            //
            // console.log(Object.keys(meta.fields));
        });
    });
});
